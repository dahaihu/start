package anything

/**
* @Author: 胡大海
* @Date: 2019-08-09 10:42
* A programmer who subconsciously views himself as an artist will enjoy what he does and will do it better ​
 */

func ttt() {
	//roles, _ := e.model["g"]["g"].RM.GetRoles(parameters["r_sub"].(string), "app1")
	//for idx, role := range roles {
	//	log.LogPrint(fmt.Sprintf("role %v is ", idx), role)
	//}
	//
	//if ok, _ := e.model["g"]["g"].RM.HasLink(parameters["r_sub"].(string), parameters["p_sub"].(string), "app1");
	//	ok && parameters["r_sub"].(string) != parameters["p_sub"].(string) {
	//	for _, policy := range e.model["g"]["g"].Policy {
	//		if parameters["r_sub"] == policy[0] {
	//			parameters["p_ext"] = policy[3]
	//			break
	//		}
	//	}
	//}
}