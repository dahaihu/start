package leetcode

import (
	"fmt"
	"testing"
)

func Test_maxProduct(t *testing.T) {
	fmt.Println(maxProduct([]int{1, 2, 3}))
	fmt.Println(maxProduct([]int{2, 3, 2, -4}))
	fmt.Println(maxProduct([]int{2, 3, 2, -4}))
}
